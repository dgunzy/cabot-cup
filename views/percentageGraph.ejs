<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team Performance Chart</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body>
    <canvas id="teamPerformanceChart" width="400" height="400"></canvas>

    <script>
        var golfersData = <%- JSON.stringify(golfers) %>;

        var golferNames = golfersData.map(function (golfer) {
            return golfer.name;
        });

        var winningPercentages = golfersData.map(function (golfer) {
            console.log(golfer.calculateWinningPercentage());
            return golfer.calculateWinningPercentage();
        });

        var ctx = document.getElementById('teamPerformanceChart').getContext('2d');
        var teamPerformanceChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: golferNames,
                datasets: [
                    {
                        label: 'Winning Percentage',
                        data: winningPercentages,
                        borderColor: 'rgba(75, 192, 192, 1)',
                        borderWidth: 1,
                        fill: false
                    }
                ]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true
                    }
                },
                responsive: true,
                maintainAspectRatio: false
            }
        });
    </script>
</body>

</html>
